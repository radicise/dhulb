package Dhulb;

import java.nio.file.Path;

public class Preprocessor {
    // takes file contents and the directory path where the files are stored and from which relative paths should be constructed
    public static String processData (String data, Path cwd) throws Exception {
        return "";
    }
}
